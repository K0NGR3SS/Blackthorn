# Development / test dependencies (not needed to run Blackthorn).
#   pip install -r requirements-dev.txt
-r requirements.txt
pytest>=8.0.0
pytest-cov>=5.0.0
ruff>=0.5.0
mypy>=1.10.0
pre-commit>=3.7.0
